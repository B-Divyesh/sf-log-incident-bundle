[
  {
    "id": "portable-html",
    "claim": "Writes one self-contained review copy with search, CSV download, and source-hash provenance.",
    "where": "landing facts, README",
    "test": "npm test -- --grep @claim:portable-html",
    "sandbox": "generate a bounded, correlated artifact from shipped sample data; open it from file: and exercise rows, search, CSV, and source hash"
  },
  {
    "id": "default-redaction",
    "claim": "The default rules replace email addresses, bearer tokens, common secret fields, and AWS-style access keys.",
    "where": "README redaction section",
    "test": "npm test -- --grep @claim:default-redaction",
    "sandbox": "generate a browser-visible artifact from a structured fixture and assert all named secret values are absent"
  },
  {
    "id": "local-processing",
    "claim": "Demo log data stays on this device.",
    "where": "demo banner, README",
    "test": "npm test -- --grep @claim:local-processing",
    "sandbox": "fresh browser context at /demo; record every request origin"
  },
  {
    "id": "csv-download",
    "claim": "Download the sample review as CSV.",
    "where": "demo toolbar",
    "test": "npm test -- --grep @claim:csv-download",
    "sandbox": "fresh browser context at /demo; download and inspect the CSV"
  },
  {
    "id": "demo-cli",
    "claim": "The CLI demo creates the six-record correlated checkout review.",
    "where": "README demo section, /demo",
    "test": "npm test -- --grep @claim:demo-cli",
    "sandbox": "run --demo from a clean checkout, parse its JSON result, and open the generated artifact"
  }
]
